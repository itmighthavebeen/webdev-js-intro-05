// Prevent us from attempting to use variables
// that are not declared
/*"use strict"
let bachPieces = ["BWV 1: Wie schön leuchtet der Morgenstern (How Brightly Shines)", 
    "BWV 2: Ach Gott, vom Himmel sieh darein ",
    "BWV 3: Ach Gott, wie manches Herzeleid", 
    "BWV 4: Christ lag in Todesbanden"];
    
    for (let i = 0; i < bachPieces.length; i++) {
        console.log(bachPieces[i]);
      }

    

bachPieces.forEach(item => {
  console.log(item);
});*/
/*let bachPieces = [
  "Toccata and Fugue in D minor", // String
  12,                             // Number
  true,                           // Boolean
  { composer: "Johann Bach", year: 1727 }, // Object
  [ "Allegro", "Largo", "Presto" ], // Array
];

console.log(bachPieces);
bachPieces.forEach(item => {
  console.log(typeof item);
});
bachPieces.pop(); 
console.log(bachPieces);

bachPieces.splice(2, 1); 
console.log(bachPieces);
// Add an item to the end of the array
bachPieces.push("Air on the G String");
console.log(bachPieces);

// Add an item to the beginning of the array
bachPieces.unshift(1727); 
console.log(bachPieces);
// Change the value of the element at index 0 (the first element, a string)
bachPieces[0] = "Brandenburg Concerto No. 3";
console.log(bachPieces); */





